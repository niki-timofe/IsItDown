It looks like <%= link_to @site, "http://#{@site}", :target => '_blank' %> is
<% case @cached['code'] when -1 %>
    <% if @cached['cached_at'].blank? %>
        <span class="down">down</span>.
    <% else %>
        <span class="down" title="<%= time_ago_in_words Time.at @cached['cached_at'] %> ago">down</span>.
    <% end %>
<% else %>
    <% if @cached['cached_at'].blank? %>
        <span class="up">up</span>.
    <% else %>
        <span class="up" title="<%= time_ago_in_words Time.at @cached['cached_at'] %> ago">up</span>.
    <% end %>
<% end %>
<br>
<%= link_to 'Check', root_path %> another?